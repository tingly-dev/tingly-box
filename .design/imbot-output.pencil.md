# ImBot 输出铅笔画 — ASCII mockups of the predicted message flow

> 配套文档：`.design/imbot-output.md`（文字版分析 + 代码依据）。这份文档只做一件事——把那份分析里描述的消息序列画成文本框线图，方便直接"看"出来现在会是什么样子，而不是靠脑补。**不是真实截图**，是按代码逐条还原的示意；气泡文案、按钮文案都和实际发送的字符串一致（见 `imbot-output.md` 引用的代码行）。
>
> **更正（写在实现阶段发现，晚于本文档初版）**：下面"画 A"和"画 B"的平台归类有两处错——① Lark 实际上和 Feishu 用的是同一套 `Restate` 实现（`lark.Bot` 内嵌 `*feishu.Bot`，方法自动继承），应该归进画 A，不是画 B；② Discord / Slack 的问题比"画 B"画的更严重——**按钮从来没有渲染过**（不是"渲染了但用完还留着"），而且因为能力声明谎报支持交互，连画 D 那种文字兜底说明都不会出现，用户看着一段无法回复的纯文字。下面各画的文案保留原状（仍然真实反映"按钮长什么样"这个局部细节），但平台分类以 `.design/imbot-output.md` §6 更正后的表格为准，不要按这里的画法对号入座。

## 场景设定

所有下面的画都用同一个最小场景，和 `imbot-output.md` §3 一致：

- 用户发：`@cc 把 foo.go 里的错误处理改一下`
- 这一轮触发了 1 次权限确认（`Edit` 工具）+ 1 次工具调用
- 用户点了"允许"

## 图例

```
🤖 = 机器人头像/消息来源       ⚠️N = 对应 imbot-output.md 里编号的问题点
──▶ = "用户做了个动作之后"     [......] = 按钮
```

---

## 平台原型 A：Telegram（🟢 支持原地编辑，体验最好的一档）

Feishu 的权限卡片部分和这个几乎一样（都实现了 `Restate`），差别只在按钮渲染成"卡片"而不是"inline keyboard"，其余消息序列完全相同——所以下面只画 Telegram 一份，Feishu 见"平台差异速查表"。

```
┌───────────────────────────────────────────┐
│ 🤖 my-bot                              ⋮  │
├───────────────────────────────────────────┤
│                                             │
│  ┌───────────────────────────────────┐    │
│  │ ⏳ CC: Processing new session...    │    │  ← 消息①（固定发送）
│  │ ───────────────                     │    │
│  │ 💬 @cc                              │    │
│  │ 📁 my-project                       │    │
│  └───────────────────────────────────┘    │
│                                             │
│  ┌───────────────────────────────────┐    │
│  │ 🔐 Tool Permission Request          │    │  ← 消息②（带按钮）
│  │ Tool: `Edit`                        │    │
│  │                                      │    │
│  │  [✅ Allow]  [❌ Deny]  [🔄 Always]  │    │
│  └───────────────────────────────────┘    │
│                                             │
│              ──▶ 用户点 [✅ Allow]          │
│                                             │
│  ┌───────────────────────────────────┐    │
│  │ 🔐 Tool Permission Request          │    │  ← 消息②被"原地改写"
│  │ Tool: `Edit`                        │    │    （按钮消失，Restate 生效）
│  │                                      │    │
│  │  ✅ Approved                        │    │
│  └───────────────────────────────────┘    │
│                                             │
│  ┌───────────────────────────────────┐    │
│  │ ✅ Allow · for tool: `Edit`   ⚠️1   │    │  ← 消息③：多出来的重复确认
│  └───────────────────────────────────┘    │    （和消息②的内容重复，见下）
│                                             │
│  ┌───────────────────────────────────┐    │
│  │ 🔧 2 tool call(s)                   │    │  ← 消息④：工具调用聚合
│  │ • 🔧 Edit foo.go                    │    │
│  │ • ↳ ok                              │    │
│  └───────────────────────────────────┘    │
│                                             │
│  ┌───────────────────────────────────┐    │
│  │ 已经把 foo.go 的错误处理改好了，     │    │  ← 消息⑤：真正的回答
│  │ 测试通过。                          │    │    （用户唯一想看的内容）
│  └───────────────────────────────────┘    │
│                                             │
│  ┌───────────────────────────────────┐    │
│  │ [RESULT] SUCCESS               ⚠️2 │    │  ← 消息⑥：调试风格的统计块
│  │ Duration: 4231ms (API: 3900ms)      │    │    （quiet 模式也照样发）
│  │ Cost: $0.0142                       │    │
│  │ Tokens: 812 in, 305 out             │    │
│  └───────────────────────────────────┘    │
│                                             │
│  ┌───────────────────────────────────┐    │
│  │ ✅ Task done. Continue or /help.    │    │  ← 消息⑦：Task-done 卡片
│  │ ───────────────                     │    │    （footer 和消息①重复了一遍）
│  │ 💬 @cc                              │    │
│  │ 📁 my-project                       │    │
│  │  [🗑 Clear] [📁 CD] [🔧 Project]     │    │
│  └───────────────────────────────────┘    │
│                                             │
└───────────────────────────────────────────┘
```

**这是所有平台里体验最好的一档，最终也还是 7 条独立消息。** ⚠️1/⚠️2 见文末"问题点速查"。

---

## 平台原型 B：Discord（🔴 按钮从未渲染——比"残留"更差，见上方更正）

Slack、Lark 的行为和这里完全一样（都有按钮能力，但 `imbot.MessageRestater` 都没实现），只是气泡换成各自的 UI 皮肤——文字内容和"按钮残留"的问题是一模一样的，所以同样只画一份。

```
┌───────────────────────────────────────────┐
│ #general           🤖 my-bot  BOT          │
├───────────────────────────────────────────┤
│                                             │
│  🤖 my-bot                                 │
│  ⏳ CC: Processing new session...           │  ← 消息①
│  ───────────────                           │
│  💬 @cc  📁 my-project                     │
│                                             │
│  🤖 my-bot                                 │
│  🔐 Tool Permission Request                │  ← 消息②
│  Tool: `Edit`                              │
│   [✅ Allow] [❌ Deny] [🔄 Always]          │
│                                             │
│              ──▶ 用户点 [✅ Allow]          │
│                                             │
│  🤖 my-bot                                 │
│  🔐 Tool Permission Request         ⚠️3   │  ← 消息②原样留在这里，
│  Tool: `Edit`                              │     按钮还在、还能再点一次
│   [✅ Allow] [❌ Deny] [🔄 Always]  ←还在！ │     （没有任何东西告诉你
│                                             │      它已经处理过了）
│  🤖 my-bot                                 │
│  ✅ Allow · for tool: `Edit`        ⚠️1   │  ← 消息③：新发的确认消息
│                                             │
│  🤖 my-bot                                 │
│  🔧 2 tool call(s)                         │  ← 消息④
│  • 🔧 Edit foo.go                          │
│  • ↳ ok                                    │
│                                             │
│  🤖 my-bot                                 │
│  已经把 foo.go 的错误处理改好了，测试通过。  │  ← 消息⑤
│                                             │
│  🤖 my-bot                                 │
│  [RESULT] SUCCESS                   ⚠️2   │  ← 消息⑥
│  Duration: 4231ms (API: 3900ms)            │
│  Cost: $0.0142                             │
│  Tokens: 812 in, 305 out                   │
│                                             │
│  🤖 my-bot                                 │
│  ✅ Task done. Continue or /help.          │  ← 消息⑦
│  ───────────────                           │
│  💬 @cc  📁 my-project                     │
│   [🗑 Clear] [📁 CD] [🔧 Project]           │
│                                             │
└───────────────────────────────────────────┘
```

**和 Telegram 内容几乎一样多（7 条），但多背一个包袱：消息②那个按钮永远留在频道里，几天后往上翻聊天记录，用户还能点到一个"已经过期"的权限按钮。**

---

## 平台原型 C：Feishu 互动卡片（🟢 支持原地改写，企业场景）

和 Telegram 消息序列完全一致，唯一的可视化差异是权限提示渲染成"卡片"（有边框、有标题栏），而不是"聊天气泡 + inline keyboard"：

```
┌───────────────────────────────────────────┐
│ 🤖 my-bot                                  │
├───────────────────────────────────────────┤
│  ⏳ CC: Processing new session...           │  ← 消息①，纯文本，同 Telegram
│  ───────────────  💬 @cc  📁 my-project    │
│                                             │
│  ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓    │
│  ┃ 🔐 权限确认                         ┃    │  ← 消息②：卡片（card）
│  ┃ ──────────────────────────────      ┃    │
│  ┃ Tool: Edit                          ┃    │
│  ┃                                       ┃    │
│  ┃  [ ✅ Allow ] [ ❌ Deny ] [ 🔄 Always ]┃    │
│  ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛    │
│                                             │
│              ──▶ 用户点 [✅ Allow]          │
│                                             │
│  ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓    │
│  ┃ 🔐 权限确认                         ┃    │  ← 卡片被 patch 更新，
│  ┃ ──────────────────────────────      ┃    │     按钮消失
│  ┃ ✅ 已同意                            ┃    │
│  ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛    │
│  ✅ Allow · for tool: `Edit`        ⚠️1   │  ← 消息③：还是多发了一条
│                                             │
│  ( 后面④⑤⑥⑦ 和 Telegram 完全一样，省略 )   │
│                                             │
└───────────────────────────────────────────┘
```

---

## 平台原型 D：DingTalk（🔴 无按钮能力，权限确认退化成文字）

Weixin、WeCom、WhatsApp 是同一个类别——`SupportsInteraction()` 全部为 `false`，权限/多选题一律走"编号列表 + 文字回复"这条路，画一份就够代表这四个平台。Weixin 目前额外被 `SuppressVerbose` 强制关掉了详细过程（见下方备注），所以它的真实序列会比这张图更短。

```
┌───────────────────────────────────────────┐
│ 🤖 my-bot                                  │
├───────────────────────────────────────────┤
│  ⏳ CC: Processing new session...           │  ← 消息①
│  ───────────────  💬 @cc  📁 my-project    │
│                                             │
│  ┌───────────────────────────────────┐    │
│  │ 🔐 Tool Permission Request          │    │  ← 消息②：没有按钮，
│  │ Tool: `Edit`                        │    │     整段是纯文字
│  │                                      │    │
│  │ 回复以下内容之一：                   │    │  ⚠️4
│  │  • y  - 允许                        │    │
│  │  • n  - 拒绝                        │    │
│  │  • a  - 总是允许                    │    │
│  └───────────────────────────────────┘    │
│                                             │
│                          用户输入 "y" ──▶  │
│                                     ┌────┐  │
│                                     │ y  │  │
│                                     └────┘  │
│                                             │
│  ┌───────────────────────────────────┐    │
│  │ ✅ Allow · for tool: `Edit`         │    │  ← 消息③（这里原本的“弹窗”
│  └───────────────────────────────────┘    │    消息②本来就没法收起/改写，
│                                             │    所以体验上没有 Discord 那种
│  ( 后面④⑤⑥⑦ 和前面一样 )                  │    "按钮残留"，但要求用户手打
│                                             │    指令本身就是更重的交互)
└───────────────────────────────────────────┘
```

---

## 平台差异速查表（对应上面四份画）

| 属于哪种画法 | 平台 | 权限提示形式 | 用完后会怎样 |
|---|---|---|---|
| 画 A（Telegram） | Telegram | 气泡 + inline 按钮 | 原地改写，按钮消失 |
| 画 A（同序列，UI 皮肤不同） | Feishu | 见画 C（卡片皮肤） | 卡片 patch，按钮消失 |
| 画 A（**更正：和 Feishu 同一套实现**） | Lark | 同画 C——`lark.Bot` 内嵌 `*feishu.Bot`，`Restate` 是继承来的方法，不是单独实现的 | 卡片 patch，按钮消失；上一版文档把它错归进"画 B / 未接 Restate"，已更正 |
| 画 B（**更正：比图上画的更差**） | Discord / Slack | 声明了按钮能力（`components`/`blockKit`），但 `SendMessage` 代码从未把 `opts.Actions` 渲染出来——**下面画里的按钮实际上不会出现**，且因为能力声明误判为"支持交互"，连画 D 那种文字兜底说明也不会追加 | 用户看到一段无法判断如何回复的纯文字，不是"按钮残留"，是"回复方式本身缺失"；等会话超时走默认策略 |
| 画 D（DingTalk） | DingTalk / WeCom / WhatsApp | 纯文字编号列表，要求手打回复 | 没有按钮残留问题，但每次都要打字；这是能力声明和实现一致的正确降级 |
| 画 D（更短） | Weixin | 同 DingTalk 的文字降级，**且已被 `SuppressVerbose` 强制静音**，实际只会看到①②③⑦，中间的工具聚合/详细过程不会发 | 四条消息里最"干净"的一档，是唯一被专门治理过的平台 |

---

## Before / After：如果要精简，大致会变成什么样（讨论稿，非定案）

只画 Telegram 这一个平台做示意——用来说明"如果做 `.design/imbot-output.md` §7 里提的第 1、2、3 条优化，视觉上会差多少"，不代表最终会这么改。

```
  现状（7 条消息）                    一种可能的精简版（3 条消息）
┌─────────────────────┐          ┌─────────────────────┐
│ ⏳ Processing...      │          │ 🔐 权限确认           │
│ ─────── 💬 @cc 📁      │          │ Tool: Edit            │
├─────────────────────┤          │  [✅][❌][🔄]          │
│ 🔐 权限确认(带按钮)    │          ├─────────────────────┤
├─────────────────────┤          │ ✅ 已同意              │
│ 🔐 权限确认✅已同意     │◀ 原地改写  │ 🔧 Edit foo.go · ok    │  ← 原地改写：
├─────────────────────┤          │                        │    权限卡片直接
│ ✅ Allow·for tool ⚠️1 │◀ 去掉      │                        │    变成结果卡片，
├─────────────────────┤          │                        │    不再单独发
│ 🔧 2 tool call(s)     │          ├─────────────────────┤
├─────────────────────┤          │ 已经把 foo.go 的错误处   │
│ 已经改好了，测试通过    │          │ 理改好了，测试通过。      │
├─────────────────────┤          │ ✅ 完成 · 4.2s · $0.014 │  ← Cost/Duration
│ [RESULT] SUCCESS ⚠️2  │◀ 折叠进回答 │ [🗑][📁][🔧]           │    折成回答尾部
├─────────────────────┤          └─────────────────────┘    一行小字，而不是
│ ✅ Task done + footer  │◀ 去掉重复                            独立一条消息
│  [🗑][📁][🔧]          │  footer
└─────────────────────┘
```

对应 `.design/imbot-output.md` §7：① = 去掉重复确认消息，② = `[RESULT]` 折叠成回答尾部的一行小字（而不是独立方括号调试块），③ = Processing 横幅与 Task-done 卡片合并成一条会被原地改写的消息。三个改动叠加，Telegram 上从 7 条降到 3 条；不支持编辑的平台（Discord 等）因为没有"原地改写"这个能力，降到的是 4 条（少了①③的合并收益，但仍然去掉①②两处冗余）。

---

## 问题点速查（对应画里的 ⚠️ 编号）

1. **⚠️1 重复确认**：`✅ Allow · for tool: \`Edit\`` 这条消息和"权限卡片被改写成✅ Approved"是两处完全独立的代码（`prompt_reply.go` 的 `send(...)` 和 `imprompter.go` 的 `editPromptToResult`），**所有平台都会重复发**，包括体验最好的 Telegram/Feishu。
2. **⚠️2 调试风格的 `[RESULT]` 块**：`[SYSTEM]` `[RESULT]` 这种方括号标签是给人调试用的格式，直接进了聊天窗口；且这条消息**不受 quiet 模式过滤**，关了 verbose 也照样发。
3. **⚠️3 按钮永久残留**：这张图画的是"按钮发出后是死的，但长得像还能点"，对 DingTalk / Weixin / WeCom / WhatsApp 不适用（它们根本不渲染按钮，见 ⚠️4）；真正符合"按钮渲染了、用完却没收回去"这个描述的，实际平台里一个都没有——Telegram/Feishu/Lark 会原地改写收回按钮，Discord/Slack 干脆从不渲染按钮（不是"残留"，是"缺失"，见文档开头的更正和 imbot-output.md §4 第 5 条）。这张画留作"如果一个平台只实现了按钮渲染、没实现 Restate，会是什么样"的通用示意。
4. **⚠️4 文字降级**：DingTalk / Weixin / WeCom / WhatsApp 没有按钮能力，权限确认和多选题一律退化成"编号列表 + 手打回复"，比按钮更占屏幕、也更麻烦。

详细代码依据见 `.design/imbot-output.md` 对应章节。

---

## 新增：一个 chat 里两个来源同时 pending 时，回复该给谁（`msg-session-identity` → `reply-to` 系列分支）

以上所有画的都是"一条消息长什么样"。这一节画的是另一件事——**同一个 chat 里，两个互不相干的来源同时想让用户回答，系统怎么分辨"这句回复该给谁"**。背景、分析、结论、设计见 `.design/imbot-output.md` §8；代码分两阶段：Source 优先级兜底（分支 `claude/gifted-feynman-lwrxtb-msg-session-identity`，commit `cab1786`、`440946f`），原生 reply-to 精确匹配（分支 `claude/gifted-feynman-lwrxtb-reply-to`，commit `91eb37c`、`5d7f5b7`、`de0c9f3`）。

### 场景：一个 chat，两个同时 pending 的请求

```
用户 Alice 的手机
┌───────────────────────────────────────────┐
│ 🤖 my-bot                                  │
├───────────────────────────────────────────┤
│                                             │
│  Alice 正在和 @cc 聊天，改一个 bug：         │
│                                             │
│  ┌───────────────────────────────────┐    │
│  │ 🔐 Tool Permission Request          │    │  ← 消息①
│  │ Tool: `Edit`                        │    │     Source = remote_agent
│  │  [✅ Allow] [❌ Deny] [🔄 Always]    │    │     SessionID = "sess-cc-7"
│  └───────────────────────────────────┘    │     （Flow B：@cc 执行中）
│                                             │
│  与此同时，Alice 自己电脑上还跑着一个独立的   │
│  Claude Code，hook 也配到了同一个 chat：     │
│                                             │
│  ┌───────────────────────────────────┐    │
│  │ 🔐 Tool Permission Request          │    │  ← 消息②
│  │ Tool: `Bash`                        │    │     Source = notify
│  │  [✅ Allow] [❌ Deny] [🔄 Always]    │    │     SessionID = "sess-hook-3"
│  └───────────────────────────────────┘    │     （Flow A：外部 hook 进程）
│                                             │
│  Alice 没注意到有两条，直接回了一句：         │
│  ┌───────────────────────────────────┐    │
│  │ y                                   │    │  ← 纯文字，不知道点了哪个按钮
│  └───────────────────────────────────┘    │     对应的按钮
└───────────────────────────────────────────┘
```

### "y" 这句话现在怎么被处理

```
HandlePromptTextReply(chatID="alice-dm", text="y")
        │
        ▼
GetPendingRequestsForChat("alice-dm")
        │
        │  chat 里有两条 pending：
        │  ┌───────────────────────────────┐
        │  │ req①  Source=remote_agent      │
        │  │        createdAt=10:02:00      │
        │  ├───────────────────────────────┤
        │  │ req②  Source=notify            │
        │  │        createdAt=10:02:05      │  ← 更晚创建，
        │  └───────────────────────────────┘     单纯按时间排会排前面
        │
        ▼
   排序规则（两级）：
   ① Source == remote_agent 的排最前
      （不管谁先来，"当下在聊的" 优先）
   ② 同 Source 内再按时间，最新的优先
        │
        ▼
   排序结果：[ req①(remote_agent) , req②(notify) ]
        │
        ▼
   latestReq = req①   ← "y" 被当成回答 @cc 的权限确认
                          req②（notify）继续挂着，等它自己的
                          budget/timeout 到期按策略兜底
```

### 这个规则赌对了会怎样，赌错了会怎样——以及现在的结局

```
✅ 赌对（大多数情况）：            ❌ 赌错（少数情况）：
Alice 就是在回 @cc 的确认        Alice 其实是想回那条 notify
        │                              │
        ▼                              ▼
"y" 正确地批准了 Edit           "y" 被误判成批准了 Edit，
（remote_agent 那条）           而 Alice 真正想批准的 Bash
                                （notify 那条）还在干等，
                                直到自己超时按默认策略处理
```

上面这张图画的是"只有 Source 优先级排序、没有 reply-to"时的局面——**这个局面现在只在 DingTalk 上还存在**。`claude/gifted-feynman-lwrxtb-reply-to` 分支接上了原生 reply-to 精确匹配后，Alice 只要真的点"回复"那条具体消息（而不是在 chat 里裸打字），8/9 个平台上系统就直接知道她回的是哪一条，不用再猜；只有裸打字、或者在 DingTalk 上（协议层没有回复信号），才会退回这张图画的赌法。分析、结论和设计细节见 `.design/imbot-output.md` §8"赌错会怎样"一节，这里不重复。
