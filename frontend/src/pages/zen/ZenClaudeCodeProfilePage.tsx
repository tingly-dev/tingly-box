import ClaudeCodeProfilePage from '../scenario/ClaudeCodeProfilePage';

/**
 * Zen Claude Code Profile Page
 *
 * Renders the Claude Code profile page in zen mode.
 * Reuses the existing profile page component.
 */
const ZenClaudeCodeProfilePage = () => {
    return <ClaudeCodeProfilePage />;
};

export default ZenClaudeCodeProfilePage;
