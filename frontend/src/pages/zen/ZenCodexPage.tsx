import UseCodexPage from '../scenario/UseCodexPage';

/**
 * Zen Codex Page
 *
 * Renders the actual Codex page content without redirecting.
 * This allows the zen layout to persist while showing the scenario page.
 */
const ZenCodexPage = () => {
    return <UseCodexPage />;
};

export default ZenCodexPage;
